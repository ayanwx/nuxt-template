import type { Config } from "tailwindcss";

export default {
    content: [],
    theme: {
        fontFamily: {
            mono: ["Ubuntu Mono"],
        },
    },
    plugins: [],
} satisfies Config;
